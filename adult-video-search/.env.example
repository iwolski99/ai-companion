# Optional: require this password for search/thumbnail endpoints.
# Send via header: X-Search-Password: yourpassword
# Or query: ?password=yourpassword
SITE_PASSWORD=

# Cache TTL for repeated queries (seconds). Default: 180 (3 minutes)
CACHE_TTL_SECONDS=180
