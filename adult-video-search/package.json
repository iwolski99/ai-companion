{
  "name": "adult-video-search",
  "version": "1.0.0",
  "private": true,
  "description": "Personal multi-site adult video search engine (Netlify Functions + static frontend)",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "cheerio": "^1.0.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.38.1"
  },
  "engines": {
    "node": ">=18"
  }
}
